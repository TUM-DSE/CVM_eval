
PORT := "6379"
THREADS := "8"

run-redis:
  redis-server --port {{PORT}} --protected-mode no --daemonize yes

# default port of memcached is 11211
run-memcached:
  memcached --port {{PORT}} --threads {{THREADS}} -u nobody -d
